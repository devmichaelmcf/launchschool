#CORRECT ANSWER IS NIL AS return only on line returns an empty
#line immediately. Need to return an expression.
#Method returns Yippeee!!!! with the added "!!!!".
#Since there is an explict return in the block, the block returns 
#at this point of the program and never "gets to" the last line.
